<%- include header %>
<section id="main">
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2" style="margin-top:20px;">
                <div class="well" style="padding-left:0px;padding-right:0px;padding-bottom:0px;">
                    <div class="container" style="margin-top:20px;margin-bottom:20px;margin-left:15px;">
                        <h1><%= doc.name %></h1>
                        <!-- <p>請完成以下題目</p>
                            <h3><a title="編輯" href="/txt/<%= title %>" > 編輯 </a></h3>
                            <p class="text-danger">*必填</p> -->
                        <br>
                    </div>
                    <% doc.choice.forEach(function(type, index){ %>
                    <div class="col-md-13 col-md-offset-0" id="container<%= index %>">
                        <div class="container">
                            <form action="/test/<%= doc.name %>" method="post">
                                <fieldset style="margin-left:20px;">
                                    <textarea name = "post<%= index %>" rows="<%= doc.post.test[ index ].split('\n').length %>" class="form-control" style="overflow:hidden;width:60%;" onfocus="sectionFocus(<%= index %>)" onfocusout="sectionFocusOut(<%= index %>)"> <%= doc.post.test[ index ] %> </textarea>
                                    <!-- <br/> -->
                                    <% if(type[0] == 'single' ){ %>
                                    <%for(var i =0;i < Number(type[1]); i++){%>
                                    <div class="form-group" style="margin-top:10px;">
                                        <span class="circle"></span>
                                        <div class="radio radio-primary">
                                            <label>
                                            <input type="radio" name="<%= sum %>" value="A" id="<%= sum %>A">
                                            <%= doc.post.choice[ index ][i].A %>
                                            <textarea name = "A" class="form-control" rows="1" style="display:none"> <%= doc.post.choice[ index ][i].A %> </textarea>
                                            </label>
                                        </div>
                                        <div class="radio radio-primary">
                                            <label>
                                            <input type="radio" name="<%= sum %>" value="B" id="<%= sum %>B">
                                            <%= doc.post.choice[ index ][i].B %>
                                            <textarea name = "B" class="form-control" rows="1" style="display:none"> <%= doc.post.choice[ index ][i].B %> </textarea>
                                            </label>
                                        </div>
                                        <div class="radio radio-primary">
                                            <label>
                                            <input type="radio" name="<%= sum %>" value="C" id="<%= sum %>C">
                                            <%= doc.post.choice[ index ][i].C %>
                                            <textarea name = "C" class="form-control" rows="1" style="display:none"> <%= doc.post.choice[ index ][i].C %> </textarea>
                                            </label>
                                        </div>
                                        <div class="radio radio-primary">
                                            <label>
                                            <input type="radio" name="<%= sum %>" value="D" id="<%= sum %>D">
                                            <%= doc.post.choice[ index ][i].D %>
                                            <textarea name = "D" class="form-control" rows="1" style="display:none"> <%= doc.post.choice[ index ][i].D %> </textarea>
                                            </label>
                                        </div>
                                        <div id="btn-group-<%= index %>" style="display:none">
                                            <button onclick="removeText('single',<%= index%>,<%= i%>,'<%= doc.name%>',<%= Number(type[1])-1 %>)" type="button" class="btn btn-default" id="btn-<%= sum %>">刪除此題</button>
                                            <button onclick="insert('<%= doc.name%>',<%= index%>)" type="button" class="btn btn-default" id="btn-<%= sum %>">新增下一題</button>
                                            <button onclick="single(<%= index%>,'<%= doc.name%>',<%= Number(type[1])+1 %>)" type="button" class="btn btn-default" id="btn-<%= sum %>">新增單選</button>
                                            <button onclick="multiple(<%= index%>,'<%= doc.name%>',<%= Number(type[1])+1 %>)" type="button" class="btn btn-default" id="btn-<%= sum %>">新增多選</button>
                                            <button class="btn btn-default" id="btn-<%= sum %>">儲存變更</button>
                                        </div>
                                        <% sum = sum +1; %>
                                    </div>
                                    <% } %>
                                    <% } %>
                                    <% if(type[0] == 'multiple' ){ %>
                                    <%for(var i =0;i < Number(type[1]); i++){%>
                                    <div class="form-group" style="margin-top:10px;">
                                        <div class="checkbox">
                                            <label id="check">
                                                <input type="checkbox" style="padding-right:5px">
                                                <span id="check-item">A<%= doc.post.choice[ index ][i].A %></span>
                                                <textarea name = "A" rows = "1" style="display:none"> <%= doc.post.choice[ index ][i].A %> </textarea>
                                            </label>
                                        </div>
                                        <div class="checkbox">
                                            <label id="check">
                                                <input type="checkbox" style="padding-right:5px">
                                                <span id="check-item">B<%= doc.post.choice[ index ][i].B %></span>
                                                <textarea name = "B" rows = "1" style="display:none"> <%= doc.post.choice[ index ][i].B %> </textarea>
                                            </label>
                                        </div>
                                        <div class="checkbox">
                                            <label id="check">
                                                <input type="checkbox" style="padding-right:5px">
                                                <span id="check-item">C<%= doc.post.choice[ index ][i].C %></span>
                                                <textarea name = "C" rows = "1" style="display:none"> <%= doc.post.choice[ index ][i].C %> </textarea>
                                            </label>
                                        </div>
                                        <div class="checkbox">
                                            <label id="check">
                                                <input type="checkbox" style="padding-right:5px">
                                                <span id="check-item">D<%= doc.post.choice[ index ][i].D %></span>
                                                <textarea name = "D" rows = "1" style="display:none"> <%= doc.post.choice[ index ][i].D %> </textarea>
                                            </label>
                                        </div>
                                        <div id="btn-group-<%= index %>" style="display:none">
                                            <button onclick="removeall('<%= doc.name%>',<%= index%>)" type="button" class="btn btn-default" id="btn-<%= sum %>">刪除此題</button>
                                            <button onclick="insert('<%= doc.name%>',<%= index%>)" type="button" class="btn btn-default" id="btn-<%= sum %>">新增下一題</button>
                                            <button onclick="single(<%= index%>,'<%= doc.name%>',<%= Number(type[1])+1 %>)" type="button" class="btn btn-default" id="btn-<%= sum %>">新增單選</button>
                                            <button onclick="multiple(<%= index%>,'<%= doc.name%>',<%= Number(type[1])+1 %>)" type="button" class="btn btn-default" id="btn-<%= sum %>">新增多選</button>
                                            <button class="btn btn-default" id="btn-<%= sum %>">儲存變更</button>
                                        </div>
                                        <% sum = sum +1; %>
                                    </div>
                                    <% } %>
                                    <% } %>
                                </fieldset>
                            </form>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </div>
</section>
<%- include footer %>
